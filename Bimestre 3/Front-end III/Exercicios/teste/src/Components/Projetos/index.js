import './style.css';

const Projetos = ({children}) => {
    return(
        <>
        <div className="projetos">
            {children}
        </div>
        </>
    );
}

export default Projetos;